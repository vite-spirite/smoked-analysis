import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vitespirite.smokedanalysis',
  appName: 'Smoked Analysis',
  webDir: 'dist',
  bundledWebRuntime: false,
  backgroundColor: 'ffffff' 
};

export default config;
